#include "select_image_point_page.h"

SelectImagePointPage::SelectImagePointPage(QWidget* parent)
    : TrWidget(parent)
{
    ui.setupUi(this);

    updateText();
}

void SelectImagePointPage::updateText()
{}
